#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_@vue_cli@4.5.15@@vue/cli/bin/vue.js" "$@"
  ret=$?
else 
  node  "$basedir/../_@vue_cli@4.5.15@@vue/cli/bin/vue.js" "$@"
  ret=$?
fi
exit $ret
