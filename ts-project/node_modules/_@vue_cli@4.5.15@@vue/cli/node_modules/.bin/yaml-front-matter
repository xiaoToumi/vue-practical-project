#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../../_yaml-front-matter@3.4.1@yaml-front-matter/bin/js-yaml-front.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../../_yaml-front-matter@3.4.1@yaml-front-matter/bin/js-yaml-front.js" "$@"
  ret=$?
fi
exit $ret
